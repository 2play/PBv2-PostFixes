bind E "+use"
bind space "+jump"
bind uparrow "+forward"
bind leftarrow "+left"
bind rightarrow "+right"
bind downarrow "+back"
bind W "+forward"
bind S "+back"
bind A "+left"
bind D "+right"
bind X "crouch"
bind , "+moveleft"
bind . "+moveright"
bind Mouse2 "+use"
